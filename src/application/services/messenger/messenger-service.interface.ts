export interface ITelegramService {
    // send(payload: OrganizationStatsM): Promise<void>;
    // sendSuccededPlanMessage(payload: OrganizationM): Promise<void>;
    // sendExpiredTest(id: number, companyName: string): Promise<void>;
    // sendExpiredPlan(id: number, organizationId: number): Promise<void>;
    // sendMessage(text: string): Promise<void>;
}
